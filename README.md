#### This is a simple Spygame replica built on NextJS

To use run:

- `npm install`

this will install all dependencies

- `npm build` then `npm start`

Please note: due to a bug using useReducer() this only works if built not in dev.
